import React from 'react';
import Navbar from '../NavBar/Navbar';

const NotMatch = () => {
    return (
        <div>
            <Navbar></Navbar>
            <h3>404 not match, back to home</h3>
        </div>
    );
};

export default NotMatch;